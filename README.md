# Создать простой AI агент на Python с использованием LangChain

## Requirements
- [high] Подключение к LM Studio: Добавить параметр base_url='http://localhost:1234/v1' при инициализации модели LM Studio и убедиться, что агент создаётся через create_agent.
